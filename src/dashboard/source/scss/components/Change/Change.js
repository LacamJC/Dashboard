import styles from "./Change.module.css"

const Change = () => {
    return (
        <>
            <div className={`${styles.check}`}>
                <h2>
                    Visualizar por
                </h2>
            </div>
        </>
    )
}

export default Change